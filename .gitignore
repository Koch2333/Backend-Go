# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env and sqlite file
.env
.env.*
config/aicweb/local.env
config/redirect/local.env
config/roundnfc/local.env
databases/*.db
databases/*/*.db

# Object storage (RoundNFC 本地驱动)
/storage/

# Temporary files of auto imports
autogen_imports.go

# admin SPA 构建产物；dist 下仅保留 .gitkeep。
internal/adminui/dist/*
!internal/adminui/dist/.gitkeep
